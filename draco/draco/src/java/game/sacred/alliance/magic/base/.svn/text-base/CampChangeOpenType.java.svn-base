package sacred.alliance.magic.base;

public enum CampChangeOpenType {
	
	SELF((byte)0),//当前阵营
	MAX((byte)1),//繁荣度最高的阵营
	CAN_USE((byte)2)//可以转入的阵营
	;
	
	private final byte type;//活动类型
	
	private CampChangeOpenType(byte type) {
		this.type = type;
	}

	public byte getType() {
		return type;
	}
}
