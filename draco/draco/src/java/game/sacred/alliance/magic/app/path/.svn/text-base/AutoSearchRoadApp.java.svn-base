package sacred.alliance.magic.app.path;


/**
 * 自动寻路实现接口
 * @author Wang.K
 *
 */
public interface AutoSearchRoadApp {
	
	/**
	 * 自动寻路
	 * @param srcMapId 起始地图
	 * @param arriveMapId 目标地图
	 * @param point 终止点
	 * @param raceType
	 * @return
	 * @throws Exception
	 */
//	public List<PointItem> searchRoad(String srcMapId, String arriveMapId, 
//			PointItem point, RaceType raceType) throws Exception ;
	
	/*//	 任务
	public List<PointItem> searchQuestNpc(RoleInstance p1_role,// 角色实例对象
										  int p2_questId,      // 任务ID
										  String p3_id,
										  RaceType raceType) throws Exception;*/
	
	/**
	 * 寻找NPC
	 * @param p1_userMapId
	 * @param p2_npcId
	 * @param raceType
	 * @return
	 * @throws Exception
	 */
	//public List<PointItem> searchNPC(String p1_userMapId, String p2_npcId, RaceType raceType)throws Exception ;
	
	
	/** 
	 * 返回任务目标点坐标 
	 * @param role
	 * @param questId
	 * @param id 怪npcId、收集物品id..
	 * @return
	 */
	//public Point getQuestTarget(RoleInstance role, int questId, String id);
	
	/**
	 * 任务寻路
	 * @param role
	 * @param questId 任务ID
	 * @param index 任务条件所在序列 -1：当前任务阶段 -2：接受阶段 -3：提交阶段
	 * @return
	 */
	//public List<PointItem> searchQuest(RoleInstance role, int questId, byte index) throws Exception;
	
}
